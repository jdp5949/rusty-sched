# v0.3.0 — Autosys parity core — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Date:** 2026-05-23
**Branch:** `feat/v0.3-autosys-parity-core`
**Goal:** Close the largest Autosys feature gaps (box semantics, exit-code policies, exclude calendars, must_start/complete times, condition look-back, additional condition fns) and ship Cronicle-style WebSocket live log tail.

**Architecture:** Extend `rsched-core` with new fields, evolve `rsched-conditions` grammar, add scheduler-side semantics in `rsched-scheduler`, persist via migration `0002_autosys_parity.sql`, surface in JIL parser, expose via REST + UI.

**Tech Stack:** Rust 1.79+, sqlx (sqlite + postgres), axum (REST + WS), chrono, serde, proptest, React/Tailwind UI.

---

## File Map

| File | Change |
|---|---|
| `crates/rsched-core/src/job.rs` | Add fields: `fail_codes`, `max_exit_success`, `exclude_calendar_id`, `must_start_times`, `must_complete_times` |
| `crates/rsched-core/src/box.rs` | Add `box_success_expr`, `box_failure_expr`, `box_terminator`, `job_terminator`, `auto_hold` |
| `crates/rsched-core/src/policy.rs` | Add `ExitCodePolicy` derivation helper |
| `crates/rsched-core/src/lib.rs` | Re-export new types |
| `crates/rsched-conditions/src/expr.rs` | Extend `Expr` w/ look-back time + new fns `NumRun`, `NumSuc`, `NumFail` |
| `crates/rsched-conditions/src/parse.rs` | Optional `, HH.MM` operand inside `success/failure/done` parens |
| `crates/rsched-conditions/src/eval.rs` | Look-back evaluation; `numrun/numsuc/numfail` |
| `crates/rsched-scheduler/src/condition_ctx.rs` | Pass lookback window into evaluator |
| `crates/rsched-scheduler/src/tick.rs` | Honor `exclude_calendar`, must_start, exit-code policy on dispatch |
| `crates/rsched-scheduler/src/dag.rs` | Box success/failure rollup |
| `crates/rsched-jil/src/spec.rs` | New optional fields |
| `crates/rsched-jil/src/parse.rs` | Parse new attrs |
| `crates/rsched-jil/src/translate.rs` | Map to core types |
| `crates/rsched-store/migrations/sqlite/0002_autosys_parity.sql` | New |
| `crates/rsched-store/migrations/postgres/0002_autosys_parity.sql` | New |
| `crates/rsched-store/src/repo.rs` | Read/write new columns |
| `crates/rsched-api/src/routes.rs` | `GET /api/v1/runs/:id/logs/ws` WebSocket |
| `crates/rsched-ui/assets/index.html` | Live WS-tail when run detail open |
| `NOTES.md` | Move M-items from "Deferred" to "Done" |

---

## Task 1: Exit-code policy on core

**Files:**
- Modify: `crates/rsched-core/src/job.rs`
- Modify: `crates/rsched-core/src/policy.rs`

- [ ] **Step 1: Add `ExitCodePolicy` struct in `policy.rs`**
  Fields: `max_exit_success: i32` (default 0), `fail_codes: Vec<i32>` (default empty), `condition_code: Option<i32>`.
  Method: `pub fn evaluate(&self, exit: i32) -> RunOutcome { Success | Failure | Conditional }`
- [ ] **Step 2: Add `RunOutcome` enum + tests**
  Boundaries: exit ≤ max_exit_success → Success; in fail_codes → Failure; equals condition_code → Conditional; else Failure.
- [ ] **Step 3: Add `Job::exit_policy: ExitCodePolicy` field**
  Default = `ExitCodePolicy { max_exit_success: 0, fail_codes: vec![], condition_code: None }`.
- [ ] **Step 4: `cargo test -p rsched-core` passes**
- [ ] **Step 5: Commit `feat(core): exit code policy (max_exit_success + fail_codes + condition_code)`**

## Task 2: Box semantics fields

**Files:**
- Modify: `crates/rsched-core/src/box.rs`

- [ ] **Step 1: Extend `Box` struct**
  Add: `box_success_expr: Option<String>` (condition expr — when true → Success), `box_failure_expr: Option<String>` (when true → Failure), `box_terminator: bool` (default false: if box fails, kill all running children), `job_terminator: bool` (per-job override), `auto_hold: bool` (children auto-paused when box running).
- [ ] **Step 2: Tests for validation + default**
- [ ] **Step 3: Commit `feat(core): box semantics — success/failure exprs, terminators, auto_hold`**

## Task 3: Exclude calendar on Job

**Files:**
- Modify: `crates/rsched-core/src/job.rs`

- [ ] **Step 1: Add `Job::exclude_calendar_id: Option<CalendarId>`**
- [ ] **Step 2: Test combining include + exclude calendars (helper fn)**
- [ ] **Step 3: Commit `feat(core): job.exclude_calendar_id (Autosys exclude_calendar)`**

## Task 4: must_start_times + must_complete_times

**Files:**
- Modify: `crates/rsched-core/src/job.rs`

- [ ] **Step 1: Add `must_start_times: Vec<NaiveTime>`, `must_complete_times: Vec<NaiveTime>`**
  Stored as wall-clock times (today's date). Evaluator picks earliest future occurrence.
- [ ] **Step 2: Tests for serde roundtrip**
- [ ] **Step 3: Commit `feat(core): must_start_times + must_complete_times`**

## Task 5: Condition look-back operand

**Files:**
- Modify: `crates/rsched-conditions/src/expr.rs`
- Modify: `crates/rsched-conditions/src/parse.rs`

- [ ] **Step 1: Extend `Expr::Success/Failure/Done` to carry `Option<Duration>` look-back**
  Variants become `Success(String, Option<Duration>)` etc.
- [ ] **Step 2: Parser: inside parens optional `, HH.MM` (hours.minutes)**
  Examples: `success(jobA, 00.30)`, `failure(jobB, 02.15)`, `done(j, 00.05)`.
- [ ] **Step 3: Update `Display` impl**
- [ ] **Step 4: Tests: parse + roundtrip + edge cases (negative, malformed)**
- [ ] **Step 5: Commit `feat(conditions): look-back operand HH.MM`**

## Task 6: Condition fns `numrun`, `numsuc`, `numfail`

**Files:**
- Modify: `crates/rsched-conditions/src/expr.rs`
- Modify: `crates/rsched-conditions/src/parse.rs`

- [ ] **Step 1: Add variants `NumRun(String, CmpOp, i32, Option<Duration>)` (similarly for suc, fail)**
- [ ] **Step 2: Parser entries for `numrun`, `numsuc`, `numfail`**
- [ ] **Step 3: Tests including comparison ops**
- [ ] **Step 4: Commit `feat(conditions): numrun/numsuc/numfail fns`**

## Task 7: Condition evaluator — look-back + counts

**Files:**
- Modify: `crates/rsched-conditions/src/eval.rs`
- Modify: `crates/rsched-scheduler/src/condition_ctx.rs`

- [ ] **Step 1: Extend `UpstreamState` trait**
  Add: `success_within(job, lookback)`, `failure_within(...)`, `done_within(...)`, `count_runs_within(...)`, `count_successes_within(...)`, `count_failures_within(...)`.
- [ ] **Step 2: `StoreUpstreamState` impl queries `runs` table by `started_at >= now - lookback`**
- [ ] **Step 3: Evaluator dispatches based on look-back presence**
- [ ] **Step 4: Tests with mock state**
- [ ] **Step 5: Commit `feat(conditions+scheduler): look-back + count eval`**

## Task 8: Scheduler integration — exit code policy + exclude calendar

**Files:**
- Modify: `crates/rsched-scheduler/src/dispatch.rs`
- Modify: `crates/rsched-scheduler/src/tick.rs`

- [ ] **Step 1: When run finishes, derive `RunState` via `ExitCodePolicy::evaluate(exit)`**
- [ ] **Step 2: Tick loop: if exclude_calendar allows now → skip (don't dispatch)**
- [ ] **Step 3: Tests with mock store**
- [ ] **Step 4: Commit `feat(scheduler): honor exit policy + exclude calendar`**

## Task 9: Box success/failure rollup

**Files:**
- Modify: `crates/rsched-scheduler/src/dag.rs`
- Modify: `crates/rsched-scheduler/src/tick.rs`

- [ ] **Step 1: After child run finishes, eval `box_success_expr` and `box_failure_expr` against children states**
- [ ] **Step 2: If box terminator + box failed → kill running siblings**
- [ ] **Step 3: Tests for: default (all children succeed = success), expr override**
- [ ] **Step 4: Commit `feat(scheduler): box success/failure rollup + terminator`**

## Task 10: SQL migration 0002

**Files:**
- Create: `crates/rsched-store/migrations/sqlite/0002_autosys_parity.sql`
- Create: `crates/rsched-store/migrations/postgres/0002_autosys_parity.sql`
- Modify: `crates/rsched-store/src/repo.rs`

- [ ] **Step 1: Migration adds nullable columns to `jobs`**
  `exclude_calendar_id`, `must_start_times_json`, `must_complete_times_json`, `exit_policy_json`.
- [ ] **Step 2: Migration adds columns to `boxes`**
  `box_success_expr`, `box_failure_expr`, `box_terminator`, `job_terminator`, `auto_hold`.
- [ ] **Step 3: Repo `JobRow ↔ Job` mapping handles new fields (forward + backward compat)**
- [ ] **Step 4: Repo `BoxRow` mapping**
- [ ] **Step 5: `cargo test -p rsched-store` passes (incl. migration smoke)**
- [ ] **Step 6: Commit `feat(store): migration 0002 — Autosys parity columns`**

## Task 11: JIL parser coverage

**Files:**
- Modify: `crates/rsched-jil/src/spec.rs`
- Modify: `crates/rsched-jil/src/parse.rs`
- Modify: `crates/rsched-jil/src/translate.rs`

- [ ] **Step 1: Add optional fields to `JobSpec`**: `exclude_calendar`, `must_start_times`, `must_complete_times`, `fail_codes` (CSV → `Vec<i32>`), `max_exit_success`, `condition_code`, `box_success`, `box_failure`, `box_terminator` (`y`/`n`), `job_terminator`, `auto_hold`.
- [ ] **Step 2: Parser branches**
- [ ] **Step 3: Translator maps to `Job` / `Box`**
- [ ] **Step 4: Fixture test: full insert_job w/ all new attrs round-trips**
- [ ] **Step 5: Commit `feat(jil): Autosys parity attrs (fail_codes, exclude_calendar, box_*, must_*)`**

## Task 12: WebSocket live log tail

**Files:**
- Modify: `crates/rsched-api/src/routes.rs`
- Modify: `crates/rsched-api/Cargo.toml` (axum ws feature)
- Modify: `crates/rsched-ui/assets/index.html`

- [ ] **Step 1: Add `GET /api/v1/runs/:id/logs/ws` route**
  Stream new `run_logs` rows as `{seq, ts, stream, chunk}` JSON frames. Poll DB every 250ms; emit deltas.
- [ ] **Step 2: UI: when run detail view open, open `ws://`, append lines live**
- [ ] **Step 3: Integration test: spawn server, post run w/ logs, ws client receives frames**
- [ ] **Step 4: Commit `feat(api+ui): WebSocket live log tail (Cronicle-style)`**

## Task 13: Full sweep + PR

- [ ] **Step 1: `cargo fmt --all`**
- [ ] **Step 2: `cargo clippy --workspace --all-targets -- -D warnings`**
- [ ] **Step 3: `cargo deny check`**
- [ ] **Step 4: `cargo test --workspace`**
- [ ] **Step 5: Update `NOTES.md` — move v0.3 items to "Done", note remaining (auth, agents, themes, raft, packaging) for v0.4+**
- [ ] **Step 6: `git push -u origin feat/v0.3-autosys-parity-core`**
- [ ] **Step 7: `gh pr create` w/ summary + test plan**
- [ ] **Step 8: Report PR URL to user**

---

## Self-review checklist

- ✅ Every gap-report v0.3 item has a task (box semantics → T2/T9, fail_codes → T1, exclude → T3/T8, must_times → T4, look-back → T5/T7, num fns → T6/T7, WS tail → T12, JIL → T11)
- ✅ No placeholders — every step has concrete file + change
- ✅ Type names consistent (`ExitCodePolicy`, `RunOutcome`, `Box`, `Job`)
- ✅ Migration before repo updates so types match
- ✅ Commits per task = atomic, easy review

## Out of scope (later slices)

- mTLS gRPC agent (v0.5)
- Auth / RBAC / API keys (v0.4)
- Virtual resources / machine load (v0.5)
- Event server / sendevent (v0.6)
- Themes / DAG editor (v0.7)
- Raft HA / signed packaging (v0.8)
- Chaos + mdBook (v1.0)
